class WallService {

    private val id: Int = 0

    private val wallService = WallService()

    fun add() {
// TODO: 21.07.2021 Добавить пост в массив (список) постов
    }

    fun update(post: Post): Boolean {
        if (false) {
// TODO: 21.07.2021 Пролистать список с постами и если в нём нет такого id, вернуть false
            return false
        } else {
            return true
        }
    }
}